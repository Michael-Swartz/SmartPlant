name: build
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    container:
      image: python:3.9
    steps:
      - name: Install PlatformIO
        run: pip install -U platformio
      - name: Test build
        run: ls